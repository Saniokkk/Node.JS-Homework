const checkingToken = require('./checkingToken');
const isFavorite = require('./isFavorite');
const paginationContacts = require('./paginationContacts');

module.exports = {
    checkingToken,
    isFavorite,
    paginationContacts,
}